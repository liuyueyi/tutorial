import{_ as s,V as a,W as n,a1 as d,a0 as e}from"./framework-23f3cf9b.js";const i={},r=e(`<p>通过ssh免密方式登录远程服务器, 配置比较简单，就是讲自己的rsa公钥放在远端服务器的授权文件中</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">vim</span> ~/.ssh/authorized_keys
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,2),c=e(`<h2 id="i-ssh配置" tabindex="-1"><a class="header-anchor" href="#i-ssh配置" aria-hidden="true">#</a> I. ssh配置</h2><p>首先检验是否已经生成相应的ssh文件</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-l</span> ~/.ssh
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>主要需要判断目录下是否有 <code>xxx_rsa, xxx_rsa.pub</code></p><p>如果没有则需要先生成公私钥</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>ssh-keygen <span class="token parameter variable">-t</span> rsa <span class="token parameter variable">-C</span> <span class="token string">&quot;your_email@example.com&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>然后将生成的<code>xxx.pub</code>文件中的内容，拷贝到目标服务器的 <code>~/.ssh/authoorized_keys</code>文件中即可</p>`,7);function l(t,o){return a(),n("div",null,[r,d(" more "),c])}const h=s(i,[["render",l],["__file","180707-ssh远程免密登录配置.html.vue"]]);export{h as default};
